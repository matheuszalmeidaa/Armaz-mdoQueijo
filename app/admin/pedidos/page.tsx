import { EmBreve } from "@/components/EmBreve";

export default function AdminPedidos() {
  return (
    <EmBreve
      icone="receipt_long"
      titulo="Pedidos"
      descricao="Todos os pedidos do delivery e vendas do PDV num só lugar, com status em tempo real, filtro por loja e por canal."
    />
  );
}
