import { EmBreve } from "@/components/EmBreve";

export default function AdminRelatorios() {
  return (
    <EmBreve
      icone="bar_chart"
      titulo="Relatórios"
      descricao="Vendas por loja e por canal, mais vendidos por época, ticket médio e perda de produto — os indicadores que hoje ficam no caderninho."
    />
  );
}
